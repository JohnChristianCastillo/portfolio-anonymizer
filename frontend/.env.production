# Production build: the SPA is served under the gateway at /anonymizer, so API
# calls are prefixed accordingly (the gateway gates and proxies them to this app).
VITE_API_BASE=/anonymizer/api
# Run the gateway admission handshake in production (the app is served behind the
# gateway). Off in dev, where there is no gateway.
VITE_GATEWAY_ADMISSION=1
